This is a temporary working directory for planet mesh generation.
All of the paths are relative to this directory, so be careful I guess
(although if you use the `build.ninja` file you should be fine)
